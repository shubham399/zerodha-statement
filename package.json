{
  "name": "ledger",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "sync": "node sync.js",
    "build": "npm run sync; ejs ./template/index.ejs -f ./data/ledger.json -o index.html"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "dotenv": "^10.0.0",
    "ejs": "^3.1.6"
  }
}
